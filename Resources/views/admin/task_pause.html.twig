{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}
    暂停任务 - {{ task.description }}
{% endblock %}

{% block main %}
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">任务信息</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <p><strong>任务ID:</strong> {{ task.id }}</p>
                            <p><strong>任务类型:</strong> {{ task.type.value }}</p>
                            <p><strong>任务状态:</strong>
                                <span class="badge badge-{{ task.status.value == 'in_progress' ? 'primary' : 'info' }}">
                                    {{ task.status.value }}
                                </span>
                            </p>
                        </div>
                        <div class="col-md-6">
                            <p><strong>当前优先级:</strong> {{ task.priority }}</p>
                            <p><strong>作业位置:</strong> {{ task.location }}</p>
                            <p><strong>分配作业员:</strong>
                                {% if task.assignedWorker %}
                                    作业员 #{{ task.assignedWorker }}
                                {% else %}
                                    <span class="text-muted">未分配</span>
                                {% endif %}
                            </p>
                        </div>
                    </div>
                    {% if task.description %}
                        <div class="mt-3">
                            <p><strong>任务描述:</strong></p>
                            <p class="text-muted">{{ task.description }}</p>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-pause-circle text-warning"></i>
                        暂停任务
                    </h5>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle"></i>
                        <strong>注意:</strong> 暂停任务将停止该任务的执行，直到手动恢复。请谨慎操作。
                    </div>

                    {{ form_start(form) }}
                    <div class="row">
                        <div class="col-md-6">
                            {{ form_row(form.reason) }}
                        </div>
                        <div class="col-md-6">
                            {{ form_row(form.description) }}
                        </div>
                    </div>
                    <div class="row mt-3">
                        <div class="col-12">
                            <div class="form-group">
                                {{ form_widget(form.pause) }}
                                <a href="{{ path('admin') }}" class="btn btn-secondary ml-2">取消</a>
                            </div>
                        </div>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h6 class="card-title mb-0">暂停原因说明</h6>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="border rounded p-3 mb-3">
                                <h6 class="text-danger"><i class="fas fa-tools"></i> 设备故障</h6>
                                <p class="small text-muted">相关设备出现故障，需要维修或更换</p>
                            </div>
                            <div class="border rounded p-3 mb-3">
                                <h6 class="text-warning"><i class="fas fa-boxes"></i> 物料不足</h6>
                                <p class="small text-muted">所需物料或工具不足，需要补充</p>
                            </div>
                            <div class="border rounded p-3 mb-3">
                                <h6 class="text-info"><i class="fas fa-users"></i> 人员调度</h6>
                                <p class="small text-muted">作业员需要临时调离或更换</p>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="border rounded p-3 mb-3">
                                <h6 class="text-secondary"><i class="fas fa-search"></i> 质量问题</h6>
                                <p class="small text-muted">发现质量问题，需要进一步检查</p>
                            </div>
                            <div class="border rounded p-3 mb-3">
                                <h6 class="text-danger"><i class="fas fa-exclamation"></i> 紧急插单</h6>
                                <p class="small text-muted">更高优先级的紧急任务需要立即处理</p>
                            </div>
                            <div class="border rounded p-3 mb-3">
                                <h6 class="text-primary"><i class="fas fa-clock"></i> 等待指示</h6>
                                <p class="small text-muted">等待上级或相关部门的进一步指示</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {% if task.assignedWorker %}
        <div class="row mt-4">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h6 class="card-title mb-0">作业员通知</h6>
                    </div>
                    <div class="card-body">
                        <div class="alert alert-info">
                            <i class="fas fa-bell"></i>
                            任务暂停后，系统将自动通知分配的作业员（作业员 #{{ task.assignedWorker }}）。
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}